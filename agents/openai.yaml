interface:
  display_name: "Issue Triage And Fix"
  short_description: "Map requirements to repos, triage bugs, fix, verify, and close with artifacts."
  default_prompt: "Use $issue-triage-and-fix to map my assigned bugs to the right repo, triage them, and fix safe candidates with artifact-guided verification."
